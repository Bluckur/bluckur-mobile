<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:BluckurWallet.Util" 
             x:Class="BluckurWallet.UILayer.ShopPage" 
             BackgroundColor="#f3f3f3">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:BooleanInverter x:Key="booleanInverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Button x:Name="btnCartPage" Clicked="btnCartPage_Click" HeightRequest="50" TextColor="White" BackgroundColor="#e88506" IsVisible="false"></Button>
            <ScrollView>
                <StackLayout>
                    <Grid x:Name="gridShopItems" Padding="10, 5">
                    </Grid>
                    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}">
                    </ActivityIndicator>
                    <Button Clicked="btnMoreItems_Click" Text="Load more items..." TextColor="White" BackgroundColor="#e88506" IsVisible="false"> <!-- IsVisible="{Binding IsBusy, Converter={StaticResource booleanInverter}}"-->
                    </Button>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>